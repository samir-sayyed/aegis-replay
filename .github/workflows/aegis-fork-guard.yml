name: Aegis fork guard
on: pull_request
permissions:
  contents: read
jobs:
  guard:
    if: github.event.pull_request.head.repo.fork
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python -m pip install . && aegis guard --all
